# Sample tuples used by `zed validate` together with assertions.yaml.
relationships: |
  org:acme#member@user:alice
  order:o1#org@org:acme
  order:o1#owner@user:alice
  order:o2#org@org:acme
  order:o2#owner@user:bob
  group:operator#member@user:alice
  dashboard:admin#viewer@user:alice
  dashboard:grafana#viewer@group:operator#member
  dashboard:hubble#viewer@group:operator#member
  dashboard:temporal#viewer@group:operator#member
  dashboard:minio#viewer@group:operator#member
  dashboard:argo#viewer@group:operator#member
